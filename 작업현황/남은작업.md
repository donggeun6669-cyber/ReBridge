# ReBridge 남은 작업 — 2026-06-03 기준

> 오늘까지 비전 핵심 기능(점수 비교·칸수 분석·제출서류·담임형 안내·자료부재 UI)은 앱에 모두 연결됨.
> 아래는 데이터 충실도 보강과 다듬기 위주의 후속 과제.

---

## A. 데이터 결손 (현재 소스로 한계)

### A-1. 작년 합격선 (cutlines_2025) — 보강 난이도 높음
- 현재 보유 **118개 / 결손 233개**.
- 결손 233개 중 **232개는 raw `results_2025.json`(20,830행/114대학)에도 행이 없음** = 2025 대입전형결과(대교협) PDF 자체에 미수록.
- → 현재 소스로는 못 채움. **null 유지가 정답**(추측 금지).
- 잔여 가능성: 34개 입결 PDF 커버리지 재감사로 누락분 일부 회수 가능성만.
- 추가 보강하려면 **새 입결 데이터 소스**(대학별 공시·진학사/유웨이 등) 확보 필요.

### A-2. 비교내신 (comparative_2028) — 일부 추가 가능
- patch_D 병합 후 **187개 / 결손 164개**.
- `pdf_sources/2028/`에 시행계획 PDF가 있으나 아직 미추출인 대학이 더 있으면 동일 방식(Codex 추출 → patch → `merge_2028_patchD.py` 패턴)으로 추가 병합 가능.
- 이미지/OCR 의심 1건(이화여대) — 텍스트 추출 실패분 OCR 재시도 검토.

## B. 미완 추출 작업

- **CNU(충남대) 학생부종합 수시 전형 추출** — 기존 task #8 pending. 페이지 구조 확인 후 추출 → 병합 필요.

## C. UI / 기능 다듬기 (선택)

- **칸수 게이지 정밀도**: 현재 2025 9등급제 입결 ↔ 2028 5등급제 제도 차이를 "참고용 예상"으로 라벨링. 5등급제 입결 데이터가 생기면 환산 로직 갱신.
- **제출서류**: 현재 전형 유형 기반 일반 규칙. 학교 고유 서류 데이터셋이 생기면 `documents.js` 규칙을 대학별로 정밀화(없으면 지금처럼 "모집요강 확인" 유지).
- **로드맵(RoadmapScreen)**: 일정/할 일 타임라인은 구현됨. 개인 점수·지망 대학과 연동한 맞춤 액션으로 확장 여지.
- **번들 크기**: 현재 JS 번들 1.5MB(데이터 JSON 포함). 필요 시 데이터 코드 스플리팅 / 동적 import 검토.

## D. 운영

- 배포 후 실제 모바일 기기에서 터치·스크롤·게이지 가독성 최종 확인.
- 데이터 갱신 파이프라인 문서화(어떤 스크립트가 어떤 JSON을 만드는지) — `src/data/*.py` 정리.

---

## 참고: 핵심 원칙 (계속 유지)

1. `admissions.json` / `universities.json` / `results_2025*.json` **직접 수정 금지** → 별도 patch/merged 파일로만.
2. **추측 금지** — PDF에 없는 값은 null. 잘못된 값은 빈 값보다 나쁘다.
3. 모든 데이터 행에 `source` 기록.
4. 자료가 없으면 **"없음 + 구할 수 없음"을 UI에 정직하게 표기**.
