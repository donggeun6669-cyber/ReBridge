# parse_v3 진행 상황 — 다음 세션은 여기부터 (2026-09-20 밤 갱신)

기준 지시: DEAN `03_PROJECTS/Gumgomentor/ReBridge/2026-09-19_ClaudeCode_RAW파싱_통합프롬프트.md`
및 `2026-09-20_ClaudeCode_Python파싱_전환프롬프트.md`, 교차점검 `2026-09-20_파싱진행_교차점검.md`.
범위: 파싱·중간 구조화·검수·인계 문서까지. **앱·RAW·manifest·점수엔진 수정, 커밋·푸시 금지.**

## 오늘(2026-09-20) 한 일

### 1단계 — 손실 방지 (완료)
교차점검이 지적한 병합 누락이 **사실이었고**, 점검에서 못 본 두 번째 원인도 찾았다.

| 문제 | 원인 | 조치 |
|---|---|---|
| 광운대 29→4, 광신대 23→3, 공주교대 22→6 | 인계 출력물의 필드 이름이 병합기가 읽는 이름과 달랐고, **모르는 이름을 조용히 무시**했다 | 이름 검사 + 확인된 대응표(ALIAS) + `extension_fact` 보존 → **전부 복구** |
| 계명대 31→27, 고려대(세종) 32→29 | 자연키가 좁아 `일반전형` 818명 행이 905명 행에 **덮어써짐** | 열쇠에 구분 칸 추가 + 충돌 시 순번 부여 |
| 두 경우 모두 병합기가 "성공"이라고 보고 | **행 수를 세어 대조하는 장치가 없었다** | 파일별 `원본 줄 수 = 정본 행 수` 대조 |
| 3중 이상 충돌에서 `#2` 재사용 | 세 번째가 두 번째를 덮어씀 | 겹친 개수만큼 `#2 #3 …` |
| 수능최저 `미상`인데 계산 승인 3건 | 지원자격 문장을 최저 근거로 씀 | 병합기가 `unknown` 이면 **자동 차단** → 위반 0 |
| 환산 구간 겹침 9건 | 3건은 검증기 오류(전형별 트랙 비교), 4건은 충남대 계열 구분 누락 | 같은 과목끼리만 비교 + 충남대 인문·자연/예체능 분리 → **2건**(둘 다 계산 차단) |
| 명지대 가산점이 분수 안으로 기록 | 쪽 이미지 확대 확인 결과 **나눗셈 바깥**이 맞다 | 산식·연산순서 수정. 1,000점에서 15.5점 차이 |

- 스냅샷: `work/snapshots/parse_v3_20260920_230310_before_stage1.sqlite` (1.08GB, 무결성 통과)
- 회귀검사 `regression_merge.py` **7/7 통과** (정본 안 건드리고 임시 폴더에서 실행)
- 8개 묶음 재병합: E4 1줄(서류 아닌 제외조항 — 거부가 맞다) 빼고 **전부 행 수 일치**
- 시험 사례 **8개 전부 PASS**
- 검증: 참조 0 · 근거 0 · 분류 0 · 승인 0 · 구간 2

### 2단계 — Python 자동 추출 (시험 단계, 정본 병합 안 함)
`extract_offerings.py` 신설. 자세한 내용과 다음 할 일은 **`reports/2단계_자동추출_현황.md`**.

핵심만: 표를 찾아도 **산수 검산(가로 합계 또는 세로 합계)을 통과해야만 값으로 만든다.**
검산이 불가능하면 값을 만들지 않고 예외 큐(`work/auto_out/_exceptions.jsonl`)로 보낸다.
표본 60개 문서에서 검산 통과 2,142건 · 예외 641건.

**아직 정본에 병합하지 않은 이유**: 기준을 조일 때마다 통과하는 표가 바뀐다.
원문 대조로 맞는 값(강서대 2027 경영학과 16명)이 기준 변경으로 탈락한 적이 있다.
→ **먼저 사람이 대조한 정답 묶음 20~30개를 만들고, 그걸 안전망 삼아 고쳐야 한다.**

## 현재 수치 (2026-09-20 밤)

| 항목 | 값 |
|---|---|
| 대상 대학 | 335 (문서 있는 곳 331) |
| 요강 자료 착수 | 38곳 · **기준 충족 0곳** |
| 커버리지(대학×학년도×자료 7종) | 4,515칸 중 278칸 = **6.1%** |
| 모집전형 / 자격 / 평가 / 환산 / 최저 / 일정 / 서류 | 4,595 / 533 / 466 / 74 / 358 / 319 / 137 |
| 입시결과 값 | 681,113개 · 320곳 (분류+학년도 확정 446,146 = 65%) |
| 사람이 눈으로 확인한 레코드 | 11건(`main_review.py`) |

## 다음 순서
1. **정답 묶음 만들기** — 서로 다른 형식의 표 20~30개를 원문 대조해 고정. 추출기 고칠 때마다 전부 통과해야 한다
2. 선 없는 표·합계 없는 표 대응 → 검산 통과율 올리기
3. 통과한 것만 `merge_agent.py PY-OFF` 로 병합, 행 수 대조
4. 평가·일정·서류 추출기로 확대 (환산표·수능최저는 문장이 섞여 에이전트가 맡는 편이 낫다)
5. 예외 큐를 에이전트 작업 지시서로 써서 대학 확대 재개
6. `후속_앱반영_필수규칙.md` 갱신 (앱은 건드리지 않는다)

## 대기 중
- **동근님 지시로 4단계(대학 확대)는 대기.**
- 정본 DB 에 쓰는 도구를 하나로 모을지 미정 (지금은 Claude Code 와 Antigravity 둘 다 씀)

## 사용 모델·비용
메인 Opus 5. 오늘 밤 작업은 서브에이전트 0개(전부 메인이 직접). 유료 API·유료 OCR 0.
