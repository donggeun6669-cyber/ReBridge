# 2단계 — Python 자동 추출기 현황 (2026-09-20 밤)

목표: 요강의 반복적인 표는 **Python 이 읽고**, AI(에이전트)는 **기계가 못 읽은 예외만** 맡는 구조로 바꾼다.
지금까지 요강 자료는 전부 에이전트가 한 줄씩 읽어 만든 것이라 335곳 중 38곳(칸 기준 6%)에 머물러 있었다.

## 만든 것

| 파일 | 하는 일 |
|---|---|
| `extract_offerings.py` | 요강·시행계획 PDF 에서 '모집단위 × 전형' 모집인원표를 찾아 offerings.jsonl 로 뽑는다 |
| `regression_merge.py` | 병합기가 손실·증식 없이 도는지 실제로 돌려 확인한다(7개 항목) |
| `work/auto_out/_exceptions.jsonl` | 기계가 확정하지 못한 문서·쪽·사유 목록 = **에이전트 작업 지시서** |

출력은 `work/auto_out/PY-OFF/<univId>/offerings.jsonl` 이고, 정본 반영은 기존 `merge_agent.py` 하나로만 한다.
**병합 경로를 새로 만들지 않았다.**

## 핵심 설계 — 산수로 검산되지 않으면 값으로 만들지 않는다

표를 찾았다고 바로 값으로 만들지 않는다. 두 가지 검산 중 하나를 통과해야 한다.

1. **가로 검산** — 행마다 `각 전형 인원의 합 == 그 행의 계`
2. **세로 검산** — 전형 열마다 `각 학과 인원의 합 == 맨 아래 합계 행의 값`

통과하면 `value_status: confirmed`, 검산할 합계가 아예 없으면 **값을 만들지 않고 예외 큐로 보낸다.**

왜 이렇게 했는가: 검산 없이 뽑으면 60개 문서에서 17,693건이 나왔는데 그중 산수로 확인된 것은 310건(1.8%)뿐이었다.
나머지를 정본에 넣으면 숫자만 부풀고 신뢰도는 떨어진다 — 2026-09-20 교차점검이 지적한 바로 그 실패다.

검산 장치가 실제로 잡아낸 것:
- 강원대 수시요강: 한 행에 여러 학과가 뭉쳐 들어가 인원이 어긋남 → 탈락
- 국민대 수시요강: 전형 이름이 `'25'`, 학과 이름이 `'※'` → 탈락
- 서울과기대: 학과 열 대신 **계열 열**(인문/자연)을 학과로 잡음 → 열 고르기를 '값이 서로 다른 정도'로 바꿔 해결

## 지금 성능 (표본 60개 문서)

| | 값 |
|---|---|
| 검산 통과해 값이 된 레코드 | 2,142건 |
| 검산할 합계가 없어 값으로 만들지 않은 칸 | 37,570칸 |
| 예외(사람·에이전트가 볼 것) | 641건 |
| ├ 검산할 합계 열이 없음 | 395 |
| ├ 표를 못 찾음(선 없는 표) | 149 |
| ├ 모집단위 열을 못 찾음 | 55 |
| ├ 행 합계가 맞지 않음(표 어긋남) | 40 |
| └ 전형 열을 못 찾음 | 2 |

## 아직 쓸 수 없는 이유 — 정직하게

**기준을 조일 때마다 붙는 표와 떨어지는 표가 바뀐다.** 예를 들어 합계 행 인식을 넓혔더니
전체 검산 통과는 361 → 2,142건으로 6배 늘었지만, 그 전까지 통과하던 **강서대 2027 수시 표(경영학과 16명 — 원문 대조로 맞는 값)가 탈락**했다.

즉 지금 추출기는 "맞는 값만 남긴다"는 방향은 옳지만, **어떤 표가 통과할지가 안정적이지 않다.**
이 상태로 전체에 돌려 정본에 넣으면 대학마다 있다 없다 하는 자료가 생긴다.

그래서 **2026-09-20 밤 기준으로 이 추출기의 결과는 정본 DB 에 병합하지 않았다.**

## 정답 묶음이 바로 잡아낸 것 (2026-09-20 밤 신설)

`golden/offerings_golden.jsonl` 에 **사람이 원문을 직접 본 값** 5건을 넣고 `check_golden.py --db` 로 정본과 대조했다.

```
  맞음  강서대학교 2027 경영학과 · 일반학생 = 16명
  맞음  강서대학교 2027 경영학과 · 교과우수자 = 21명
  없음  명지대학교 2027 경영학부 · 교과면접 → 기대 18명. 이 값이 아예 안 나왔다
  없음  성균관대학교 2027 인문과학계열 · 탐구인재 → 기대 25명
  없음  성균관대학교 2027 인문과학계열 · 융합인재 → 기대 30명
  2 / 5 일치
```

**내일 입시 전문가에게 보여줄 세 대학 중 두 곳의 모집인원이 정본에 없다.**
에이전트가 그 대학의 자격·평가·최저는 뽑았지만 **모집인원표는 안 뽑았다.**
커버리지 6.1% 가 무슨 뜻인지 보여주는 가장 구체적인 증거다.

→ 다음 세션에서 이 3건을 먼저 채우고, 정답 묶음을 20~30건으로 늘린다.

## 전체 실행 결과 (2026-09-20 23:30~23:47, 869/873 문서)

| | 값 |
|---|---|
| 검산 통과해 값이 된 레코드 | **10,450건** (90개 대학) |
| 정답 묶음 대조 (`check_golden.py`) | **0 / 5 일치** |

**결론: 이 추출기의 출력은 쓸 수 없다.** 1만 건을 뽑았지만, 사람이 원문에서 확인한 값 5건 중
**단 하나도 재현하지 못했다.** 산수 검산은 통과했는데 정답은 못 맞춘다는 것은,
검산이 '표 안에서 앞뒤가 맞는지'만 보고 '그 표가 맞는 표인지'는 못 본다는 뜻이다.

정본 DB 는 같은 대조에서 5/5 이다(메인이 원문을 직접 보고 넣은 값이라서다).
**기계 추출 결과를 정본에 병합하지 않은 판단이 맞았다.**

### 이번 실행에서 드러난 설계 결함 하나
예외 큐를 실행이 **다 끝난 뒤에 한 번에** 쓰게 만들어서, 중간에 멈추자 예외 기록이 남지 않았다.
→ 다음에는 문서 하나 끝날 때마다 바로 쓰도록 고친다.

## 다음에 할 일 (순서대로)

0. **왜 정답을 못 맞히는지 먼저 본다.** 강서대·명지대·성균관대 그 쪽에서 추출기가 무엇을 뽑았는지
   한 쪽씩 손으로 따라가 본다. 검산을 통과했는데 정답이 아니라면 '어느 표를 고르는가'가 틀린 것이다.
1. **고정 정답 묶음을 먼저 만든다.** 사람이 원문과 대조한 표 20~30개(대학·형식이 서로 다른 것)를 정답으로 두고,
   추출기를 고칠 때마다 이 묶음이 **전부** 통과하는지 확인한다. 지금은 이 안전망이 없어서 고칠 때마다 다른 데가 깨진다.
   (강서대 2027 경영학과 16명, 명지대 경영학부 18명, 성균관대 인문과학계열 탐구인재 25명 등 이미 원문 대조한 값이 출발점)
2. **선 없는 표**(149건) 대응: pdfplumber 의 `table_settings` 를 글자 좌표 기반(`text` 전략)으로 바꿔 재시도.
3. **합계가 없는 표**(395건): 합계 대신 다른 검산을 쓴다 — 같은 학과가 다른 쪽(전형별 상세)에 다시 나오면 그 값과 대조.
4. 통과한 표만 `merge_agent.py PY-OFF` 로 병합하고, 행 수 대조로 손실 0 확인.
5. 그 다음에 평가방법·일정·서류 추출기로 넓힌다. 환산표·수능최저는 문장이 섞여 있어 **에이전트가 계속 맡는 편이 낫다.**

## 쓰는 법

```bash
python3 extract_offerings.py --univ uA0000079 --force      # 한 대학만
python3 extract_offerings.py --limit 60 --force --dry      # 파일 안 쓰고 성능만 확인
python3 extract_offerings.py --force                        # 전체
python3 regression_merge.py                                 # 병합기 회귀검사(정본 안 건드림)
```

---

# 2026-09-21 — 정답 묶음 15건 전부 통과, 아직 정본에 넣지 않음

## 결과

| | 2026-09-20 밤 | 2026-09-21 |
|---|---|---|
| 정답 묶음 대조 | 0 / 5 | **15 / 15** |
| 정답 묶음 크기 | 5건(대학 3곳) | 15건(대학 6곳, 표 형식 6가지) |
| 병합기 회귀검사 | 7 / 7 | 7 / 7 |

정답 묶음에 추가한 형식: 다단 머리글(인하대), 전문대 수시1·2차 한 표(오산대), **세로쓰기 머리글**(숙명여대).

## 고친 것

**표를 못 읽던 것**
- 표 머리글 판정이 입시결과 표용이라 학과 줄까지 머리글로 먹었다 → 모집인원표 전용 판정으로 교체
- 표 선이 만든 칸 밖에 숫자가 있었다(명지대 18) → 글자 좌표로 표를 다시 만든다
- 선이 아예 없는 표(강서대) → 쪽 전체를 글자 좌표로 읽은 후보를 하나 더 둔다
- 좌우 2단 표에서 머리글과 자료가 한 칸 어긋난다(성균관대) → 실제 값으로 학과 열을 찾는다
- 다단 머리글이 뭉개져 전형 이름이 잘렸다 → 격자 선택 기준을 **전형 이름이 온전한 쪽**으로
- `학부(학과)`·`전공` 줄을 학과 줄로 착각해 그 아래 세로쓰기 전형 이름 줄을 버렸다(숙명여대)
  → 일반 머리글 낱말만 이어 붙인 말은 학과가 아니다. 세로쓰기 낱자를 붙여 `숙명인재(면접형)` 복원
- 칸 대부분이 `-`(그 군에서는 안 뽑음)인 정시 표가 '숫자가 적다'고 탈락했다 → `-` 도 자료 칸으로 센다
- 각주 번호가 이름에 붙어 같은 학과가 둘로 갈라졌다(`전기공학과 3`) → 각주 표시를 떼고 원문은 근거에 남긴다

**틀린 값을 만들던 것 — 정답 묶음으로는 안 잡히는 종류**
정답 묶음은 "있어야 할 값이 있는가"만 본다. "없어야 할 값이 들어왔는가"는 못 본다. 결과를 전수 조사해 찾았다.

| 무엇 | 규모 | 막은 방법 |
|---|---|---|
| 표가 한 칸 밀려 모집단위 자리에 숫자가 들어옴(`105 66 17`) | 오산대 한 쪽 257건 | 글자 2자 미만·숫자가 글자보다 많으면 버림 |
| 작년 입시결과 표(경쟁률·예비번호)를 모집인원표로 읽음 | 명지대 148건 | 머리글에 경쟁률·충원·예비번호·환산점수가 있으면 모집인원표가 아님 |
| 같은 쪽을 여러 방식으로 읽고 결과를 전부 저장 | 인하대 한 쪽에서 좋은 것 63 + 뭉개진 것 215 | 쪽마다 전형 이름이 가장 온전한 읽기 하나만 채택 |
| 전형이 아닌 열이 전형이 됨(`정원내` `입학` `편제` `수업 연한` `가군`) | 전체 5,000건 이상 | 전형에 대해 아무것도 말해 주지 않는 열은 전형 열이 아님 |
| 학과 연락처 표를 모집인원표로 읽음(`전화번호` → 4,042명) | 43건 | 연락처·전화·팩스·홈페이지 머리글 차단 |
| 합계 행을 학과로 읽음(`합계 (정원내 모집단위: 26개)`) | 40건 | 이름이 합계로 **시작**해도 합계 행 |
| 여러 전공이 한 칸에 뭉침 | 482건 | 이름 30자 초과·낱자 3개 이상이면 모집단위로 인정하지 않음 |

**설계 결함 하나**: 예외 큐를 실행이 다 끝난 뒤 한 번에 쓰게 해 두어, 중간에 멈추면 기록이 통째로 사라졌다
(2026-09-20 실제 발생) → **문서 하나 끝날 때마다** 쓰도록 고쳤다.

## 전체 실행 기록

| 회차 | 시각 | 문서 | 만든 값 | 정답 | 판단 |
|---|---|---|---|---|---|
| 1 | 13:43~14:01 | 876 | 95,925행 / 284개 대학 | 15/15 | 전형 아닌 열 5,000건 이상 → 병합 안 함 |
| 2 | 14:03~14:20 | 876 | 90,488행 / 281개 대학 | 15/15 | 연락처·합계·뭉친 이름 남음 → 병합 안 함 |
| 3 | 14:22~14:37 | 중단 | 234개 대학까지 | — | 동근님 지시로 중단 |

## 다음에 할 일

1. 3회차 전체 실행을 끝까지 돌린다(약 18분).
2. 결과를 다시 전수 조사한다. 새 오류 종류가 안 나오면 `merge_agent.py PY-OFF` 로 정본에 넣고 **행 수 대조로 손실 0** 확인.
3. 정답 묶음을 20~30건으로 늘린다(교대, 정시 군별 표, 정원내/외 분리 표).
4. 그 다음에 평가방법·일정·서류 추출기로 넓힌다. 환산표·수능최저는 문장이 섞여 있어 에이전트가 계속 맡는 편이 낫다.

**정본 DB(`work/parse_v3.sqlite`)는 이 작업에서 한 번도 건드리지 않았다.**

---

# 2026-09-21 저녁 — 정답 묶음 27건으로, 정본 쓰기 창구 정리

## 정본 DB 에 쓰는 길을 둘로 못 박았다 (동근님 결정)

확인해 보니 규칙·조건 계열 **4,598행은 이미 전부 `merge_agent.py` 를 거쳐** 들어와 있었다
(`merge_log` 에 출처 없는 행 0건. 넣은 쪽은 A1·E1~E5·T1·T2·MAIN).
입시결과 681,113행만 표 파서가 직접 넣는데, 이쪽은 `document_id` 와 `processing_log` 로 출처가 남는
별도의 정당한 경로다. 즉 실제로 깨져 있던 게 아니라 **규칙이 문서에만 있고 검사가 없었다.**

→ `validate.py` 에 「7-2. 정본 행의 출처」 검사를 넣었다. **출처 기록 없는 행이 0이어야 한다.**
→ `AGENT_CONTRACT.md` 맨 앞에 두 경로를 적었다.

## 정답 묶음 15건 → 27건

Sonnet 에이전트 5개를 병렬로 돌려 **원문 쪽 이미지까지 열어** 확인했다. 종합은 메인이 했다.

| 대학 | 새로 들어온 형식 |
|---|---|
| 서울교육대 | 교대 — 모집단위가 하나뿐, 전형마다 3열 소표 |
| 중앙대 정시 | 모집군이 별도 열이 아니라 **행 안의 한 칸**(가/나/다) |
| 나사렛대 | 다단 머리글 + 맨 오른쪽에 진짜 합산 '합계' 열 |
| 신구대 | 전문대 수시1·2차 × 정원내/외 중첩 머리글 |

### 에이전트가 원문으로 확정해 준 두 가지

1. **나사렛대의 '합계'는 진짜 합산 열이다.** 기독교학부 24+6=30, 사회복지학부 80+5+15=100 으로 검증됨.
   '정원 내'/'정원 외'는 값이 없는 **그룹 제목**이다. → 머리글 맨 끝이 합계면 검산용 열, 중간이면 묶음 이름.
2. **시행계획에는 표가 두 종류다**(국립목포대 2028).
   - `다. 전형별 모집인원` — 행이 **전형 이름**이고 학과 구분이 없다. **모집단위표로 읽으면 안 된다.**
     (이게 '교과일반전형' 이 학과가 되고 838 이 한 학과 인원이 된 원인이다)
   - `3. 모집단위 및 전형유형별 모집인원` — 행이 학과다. 이쪽만 쓴다.

## 이번에 고친 것

| 무엇 | 왜 |
|---|---|
| 반영비율·배점 표 차단 | `전형요소별 반영비율(%)` 의 20·80 이 모집인원이 됐다 (1,733건) |
| 쪽 머리말·꼬리말 제거 | `(단위: 명)`, `2027학년도 ○○대학교 정시모집요강` 이 전형 이름이 됐다 (400건) |
| 합계 조각 구분 | 머리글 **맨 끝**이 합계면 검산용 열, 중간이면 묶음 이름 |
| 전형 이름이 학과 자리에 오는 표 차단 | 시행계획 '다. 전형별 모집인원' (`학사`·`전문학사`·`…전형`) |
| 여러 학과가 한 칸에 뭉친 것 차단 | 괄호 밖에서 학과·학부로 끝나는 토막이 셋 이상이면 한 모집단위가 아니다 |
| **선 없는 쪽도 글자 좌표로 읽는다** | 지금까지 선이 없으면 **시도조차 안 하고 버렸다** (예외 1,048건) |
| `40 명` 같은 칸도 숫자로 | 서울교대는 모집인원이 `40` 이 아니라 `40 명` 이라 통째로 버려졌다 |
| 한 줄짜리 모집인원표 인정 | 교대는 모집단위가 하나뿐이라 '머리글 1줄 + 자료 1줄' 이다 |
| 같은 쪽 여러 읽기 중 고르는 기준 | 길이가 아니라 **이름이 전형처럼 생긴 비율**을 먼저 본다 |

## 전체 실행 기록 (2026-09-21)

| 회차 | 시각 | 문서 | 만든 값 | 정답 | 판단 |
|---|---|---|---|---|---|
| 1 | 13:43~14:01 | 876 | 95,925행 / 284개 대학 | 15/15 | 전형 아닌 열 5,000건 이상 → 병합 안 함 |
| 2 | 14:03~14:20 | 876 | 90,488행 / 281개 대학 | 15/15 | 연락처·합계·뭉친 이름 남음 → 병합 안 함 |
| 3 | 19:38~19:57 | 876 | 86,915행 / 278개 대학 | 15/15 | 반영비율 1,733·합계묶음 1,267·쪽머리말 400 → 병합 안 함 |
| 4 | 19:59~20:19 | 876 | 83,900행 | **21/27** | 새 정답 12건 중 6건 실패 → 원인 넷 찾아 고침 |
| 5 | 20:26~ | 진행 중 | — | 시험 10개 대학에서 **23/27** | — |

## 아직 못 맞히는 정답 4건 (원인까지 파악됨)

| 정답 | 왜 안 나오나 |
|---|---|
| 서울교대 국가보훈 5명 | 한 쪽에 상위 전형 아래 하위 전형 2개(국가보훈 5 / 농어촌 10)가 **한 칸에 겹쳐** 있다 |
| 나사렛대 기독교학부 일반전형 24 | 같은 쪽을 읽은 격자에서 머리글이 `안내 일반학생` 으로 뭉개진다 |
| 나사렛대 사회복지학부 창의융합 15 | 같은 원인 |
| 신구대 정시 영상디자인과 수능 1 | 정시 표는 `정시소계` 와 `수시+정시총계` 가 같이 있어 행 검산이 0/20 으로 떨어진다 |

**정본 DB 는 이 작업에서도 건드리지 않았다.**

---

# 2026-09-21 밤 — 외부 교차점검이 지적한 결함 4개 수정

GPT 가 실행 중인 추출기를 교차점검해 **재현 가능한 결함 4개**를 보고했다
(`../../../../Documents/DEAN/03_PROJECTS/Gumgomentor/ReBridge/2026-09-21_Claude진행중_Python추출_교차점검.md`).
메인이 코드에서 직접 확인한 결과 **넷 다 사실이었다.** 전부 고쳤다.

| 지적 | 확인한 것 | 고친 방법 |
|---|---|---|
| ① 이어 돌리면 앞 문서 결과가 지워진다 | 사실. 대학 파일 하나에 이어 붙이면서 '이번 실행에서 처음 만나는 대학'이면 지웠다 | **문서 하나 = 파일 하나**(`doc_<id>.jsonl`), 임시 파일에 쓴 뒤 이름 교체(원자적). 대학 묶음은 실행 끝에 문서 파일들에서 다시 만든다 |
| ② 검산 실패 행도 confirmed | 사실. 표가 80% 통과하면 틀린 행까지 확정했다 | **행마다 따로 판정.** 자기 가로 검산이 틀린 행은 `pending`, 검산이 불가능하면 원문 줄 대조로, 그것도 안 되면 `pending` |
| ③ 한 쪽의 별개 표를 버린다 | 사실 | **선으로 그려진 표는 전부 쓴다.** 쪽 전체 글자 격자는 선 표가 아무것도 못 냈을 때만 대안으로 쓴다 |
| ④ 모집군이 비어도 정답 검사를 통과 | 사실. 중앙대 2027 정시 결과의 `admission_group` 이 **전부 null** 이었다 | 모집군이 **행 안의 한 칸**인 표를 인식해 행마다 군을 붙인다. `check_golden.py` 가 `phase·round·admission_group·quota_type` 까지 확인한다 |

추가 지적도 사실이었다: `row_matches_source` 가 행 전체가 아니라 **앞 24글자만** 비교해,
끝 값 999 를 888 로 바꿔도 통과했다. → 행 전체를 비교하도록 고쳤다.

곁들여 고친 것: 시기·차수를 쪽 글자가 아니라 **그 열의 머리글**에서 읽는다.
전문대 요강은 한 표에 수시1차·수시2차·정시가 같이 있어 쪽 글자로는 시기를 정할 수 없었다(오산대 3건이 `phase=null`).

## 효과

- 중앙대 정시 3건이 `정시/가군` · `정시/나군` · `정시/다군` 으로 **범위까지 검증**되어 통과
- 오산대 3건이 `수시` 로 제대로 표시됨
- 국어국문학부는 자기 행 검산이 안 되어 **정직하게 `pending`** 으로 남는다
- 시험 10개 대학 5,109행 기준 `confirmed` 3,902 / `pending` 1,279

정답 검사가 **범위까지 보도록 엄격해졌는데도 23/27 유지.** 전보다 의미 있는 23이다.

## 아직 안 한 것

- 네 결함을 **실패하는 회귀검사로 고정**하는 일(교차점검 권고 2번). 다음 세션에서 `regression_merge.py` 옆에
  `regression_extract.py` 를 만든다.
- 전체(876문서) 재실행 — 고친 코드로 아직 안 돌렸다. 시험 10개 대학만 돌린 상태다.
- 남은 정답 4건: 서울교대 국가보훈 5 / 나사렛대 2건 / 신구대 정시 수능 1
- `work/auto_out/PY-OFF` 와 병합기 입력 `work/agent_out/PY-OFF` 가 다르다. 병합 전에 옮기는 절차 필요.

**정본 DB 는 오늘 하루 종일 한 번도 건드리지 않았다.**

---

# 2026-09-22 새벽 — 정본 병합 완료

## 결과

| | 병합 전 | 병합 후 |
|---|---|---|
| `offering` | 4,598 | **80,009** |
| `program` | 685 | **13,037** |
| 대학 | — | **297곳** |
| 모집군이 있는 행 | 0 | **10,059** |
| 캠퍼스가 있는 행 | 0 | 춘천 1,150 · 수원 649 · 서울 480 … |

```
행 수 대조: 원본(같은내용중복 제외) 75,411줄 → 정본 75,411행  ✔ 일치
같은 내용 중복 합침 2,480 / 열쇠 충돌 보존 3,024
```

검사: 정답 묶음 33/33(추출 결과) · 추출기 회귀검사 5/5 · 병합기 회귀검사 7/7 · `main_review.py` 12/12 재적용.

## 병합 경로를 바로잡았다

추출기는 `work/auto_out/<ID>/` 에 쓰고 병합기는 `work/agent_out/<ID>/` 를 읽는다.
이 차이를 모르고 `merge_agent.py PY-OFF` 를 돌리면 **0건을 읽고 '성공'이라고 보고한다**
(2026-09-21 외부 교차점검 지적). → `stage_auto.py` 신설.

- 준비 영역의 같은 ID 폴더를 **비우고** 시작한다(새 결과와 이전 결과를 섞지 않는다)
- 옮긴 줄 수를 원본과 **대조**한다. 달라지면 즉시 멈춘다
- 대학별·상태별 숫자를 표로 찍는다. 이 숫자가 병합기의 행 수 대조와 맞아야 한다

## 남은 판단 — `pending` 2만 건을 넣을 것인가 (동근님 결정 대기)

이번 병합에는 **검산을 통과한 77,891행만** 넣었다. `pending` 20,192행은 넣지 않았다.

그 결과 **정답 대조가 추출 결과에서는 33/33 인데 정본 DB 에서는 28/33** 이다.
빠진 5건은 전부 `pending` 이고, 그중에는 이런 것이 있다.

```
영진전문대 AI컴퓨터보안과 일반고 전형 = 24
  사유: "이 행을 따로 확인할 방법이 없어 보류. 표 검산 6/6."
```

**표 전체 검산은 통과했는데 그 행 하나만 따로 확인할 방법이 없어서** `pending` 이 됐다.
즉 `pending` 은 '틀렸다'가 아니라 '기계가 혼자 확인하지 못했다'는 뜻이다.
사람이 원문을 보고 맞다고 확인한 값 5건이 지금 정본에 없다.

| 선택 | 결과 |
|---|---|
| A. 지금처럼 `confirmed` 만 | 확실한 값만. 맞는 값도 2만 건 빠진다. **빠진 것이 어디에도 안 남는다**(예외 큐에도 없다) |
| B. `pending` 도 넣기 | 정답 33/33. 상태는 `pending` 으로 정직하게 남고, 앱·점수엔진이 그 상태를 보고 판단한다 |

메인 의견은 B다(`value_status` 칸이 바로 이것을 위해 있다). **동근님 결정 전까지는 A 상태로 둔다.**
B 로 가려면 `python3 stage_auto.py PY-OFF --include-pending` 뒤 `merge_agent.py PY-OFF`.

## 이번 실행에서 확인한 숫자

- 전체 실행: 876문서 → **98,083행 / 298개 대학**, 예외 13,517건
- 전수 조사 11개 항목 **전부 ✔ 없음**(확실히 틀린 값 0건). 2026-09-21 아침에는 5,000건 이상이었다
- 범위 칸: 군 8,816 · 캠퍼스 6,331 · 차수 16,509 — 아침에는 전부 비어 있었다
