# 2026-06-02 — 보안 프록시 · 로드맵 탭 · 배포 캐시 해결 · 협업 문서

## 한 줄
API 키 노출을 서버리스 프록시로 차단하고, "내 로드맵" 탭을 추가했으며, "배포가 반영 안 된다"의 진짜 원인(캐시)을 해결하고 팀 협업 문서를 정비했다.

## 무엇을 / 왜

### 1. 배포 캐시 문제 해결 (가장 중요)
- 증상: `vercel --prod` 직후에도 화면이 옛날로 보임.
- 원인: **Vercel 엣지 + 브라우저 캐시**가 옛 `index.html`(→옛 JS 해시)을 잠깐 서빙. 앱 복사본이 여러 개였던 것도 혼란 가중.
- 대책: `vercel.json` 헤더 — `/assets/*`는 `immutable`, `/index.html`·`/`는 `max-age=0, must-revalidate`. `npm run deploy`/`npm run verify`(캐시 우회 검증) 도구화. 프로필 하단 빌드 스탬프.
- → 상세 메모: `memory/rebridge-deploy-verify.md`

### 2. API 키 보안
- `VITE_` 접두어 키가 번들에 노출되던 문제 → `api/gemini.js`·`api/careernet.js` **서버리스 프록시** 생성, 서버 전용 키로 전환. `vercel.json`에 `/api/*` 라우트.
- (남음) 과거 노출 키 재발급 후 Vercel 환경변수 교체.

### 3. 내 로드맵 탭
- `RoadmapScreen.jsx` + `lib/roadmap.js` + `data/schedule.js` — 검정고시~대학 등록까지 개인 타임라인.

### 4. 데이터·문서
- 부산대 7전형 confirmed_detail, 인하대 confirmed 승격 + 교과추천형 오류 수정.
- 협업 README, 루트 `TODO.md`, 기획서 V2, 데이터 격차 분석, 코덱스 2025입결 추출 프롬프트.

## 커밋
`d4a5e04` `5f8b071` `3c13691` `d808e0b` `5917c4b` `89a041d` `e5e1152` `4f97833`

## 다음
- 점수 기반 합격 가능성(칸수) · 제출서류 · UI 정리 → 06-03.
