# 2026-06-03 — 점수엔진·칸수·제출서류·UI 개편 + 비교내신 patch_D / (이어서) 병렬작업 셋업·UX 수정

## 한 줄
검정고시 점수 → 비교내신 환산 → 작년 합격선 비교 → "몇 점·몇 문제 부족" → 칸수 분석 → 학교별 제출서류 → 담임형 안내까지 비전 기능을 앱 전반에 연결했고, 이어서 병렬작업(충남대·비교내신) 셋업과 UX 수정을 시작했다.

---

## 1부. (오전/직전 세션) 비전 기능 통합 — 커밋 `b890288`, `8d898d5`

자세한 내용은 `작업현황/2026-06-03_오늘작업요약.md` 참고. 요약:

- **점수 엔진(`scoreEngine.js`)**: `evaluateAdmission()`(부족 평균점수·과목당 문항수), `admissionChance()`(5단계 칸수 게이지), `comparativeAvailability()`(자료 유무 정직 분류).
- **공유 컴포넌트 `ChanceGauge.jsx`**: 상세·탐색·결과 3화면 동일 칸수 게이지.
- **자료 부재 UI**: 합격선/수능기준 없을 때 "없음 + 구할 수 없음"을 둘 다 명시.
- **비교내신 patch_D 병합(`merge_2028_patchD.py`)**: comparative 132→187, numeric_table 34→75. additive·무회귀, 빈 값은 미반영.
- **학교별 제출서류**(`documents.js` 등) DetailScreen 임베드.
- **UI 개편**: 카드 정보 과부하 제거, 상세는 탭하면 펼쳐지는 구조 + 담임 한마디 패널.
- 빌드 통과·시각 검증·배포 완료.

---

## 2부. (이어서, 이번 세션) 병렬작업 셋업 + UX 수정 시작

### 0. git pull
로컬이 origin/main보다 5커밋 뒤 → fast-forward로 최신화(`8d898d5`). 어제 작업 전부 로컬 반영.

### 1. 병렬 작업 분담 (프롬프트 작성)
- **충남대 상세 재추출** (다른 클로드 창) — `ReBridge_AI공모전용/Data/클로드_충남대_상세전형_프롬프트.md`
  - 진단: cnu 26행이 자동추출 쓰레기(모집인원 전부 null, csat/eval 원문 잘림, "목 차"가 전형으로 오분류, 수능 전형이 수시/학종으로 오분류). → 깨끗한 재추출 + patch_E replace 병합.
- **비교내신 추가 보강** (코덱스) — `ReBridge_AI공모전용/Data/코덱스_비교내신_추가보강_프롬프트.md`
  - comparative 187/351, PDF 213개 → 미추출분 추가. 합격선(cutlines)은 소스 한계로 **null 유지가 정답**(감사만).

### 2. 작업일지 폴더 신설
- 루트 `작업일지/` 생성 + 06-01~06-03 정리 + README 인덱스.

### 3. UX 수정 (사전 과제)
- **배지/칸수 혼재 수정**: "검정고시 가능"(초록 배지)이 게이지 "안정"과 같은 색이라 합격 보장처럼 보이던 문제 → eligibility를 중립 "자격" 칩으로 분리, 점수 있는데 합격선 없으면 이유 표기. (→ 본 커밋)
- **뒤로가기 수정**: 상세→뒤로가 홈으로 튀던 문제 → App.jsx 네비 스택 도입, 바로 전 화면 복귀 + 휴대폰 뒤로(popstate) 연동. (→ 본 커밋)

### 4. 도움 창구 방향 (구상)
- 챗봇 외 대안 검토 — 멘토 피드백 기반 초기 방향성 + 타 사이트 차별화 관점. (별도 메모/논의)

---

## 다음
- (본인) C. UI/기능 다듬기 — 칸수 게이지 정밀도, 지도 탭(Leaflet), 관심대학 북마크, 핵심대학 상세 6곳(국민·한양·서울과기·경북·충남·전북) 데이터.
- (병렬 결과 회수) 충남대 patch_E, 비교내신 patch_E 병합 검수.

## 핵심 원칙 (불변)
추측 주입 금지 · 자료 없으면 "없음"을 정직하게 · 행마다 source · 작업 전 pull / 후 push.
